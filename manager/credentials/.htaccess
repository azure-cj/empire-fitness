# Protect credentials directory from web access
# This prevents anyone from directly accessing the credential files via browser

# Deny all access
Order Deny,Allow
Deny from all

# Alternative method (Apache 2.4+)
# Require all denied

# Additional security: Prevent directory listing
Options -Indexes

# Prevent access to .txt files specifically
<FilesMatch "\.txt$">
    Order Deny,Allow
    Deny from all
</FilesMatch>